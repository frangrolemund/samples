// Disassembly of wall.un

// - starts execution in unum service from top of source

static var messages: [string]
// - basis gets storage allocated linked to current deployment

unum.io.print("what is your name?\n")
// - service begins listening on HTTP port when it references the grid, 
//   not before
// - a new connection comes into service and the timeline is branched so that
//   a connection has one copy of it and the service continues to block
	// TODO: explore this idea, should the main service still block?
